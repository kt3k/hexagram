<img class="gua-art {{ modifier || "" }}"
     src="{{ hexagram.image }}"
     alt="{{ hexagram.name }}の象をあらわす図"
     width="{{ hexagram.imageWidth }}" height="{{ hexagram.imageHeight }}"
     loading="{{ eager ? "eager" : "lazy" }}" decoding="async">
